// we'll throw this away, it's just some hardcoded stuff for debugging

if (Meteor.isClient) {
  
  /*
  var connection = DDP.connect("http://localhost:3000");
  duckDuckGoSearchResults = new Mongo.Collection("duckDuckGoSearchResults");
  Meteor.subscribe("rest2ddp", "duckduckgo-meteor");
  Tracker.autorun(function () {console.log(duckDuckGoSearchResults.find({}, {sort: {_id: 1}}).fetch());});
  
  weatherResults = new Mongo.Collection("weatherForecasts");
  Meteor.subscribe("rest2ddp", "weather");
  Tracker.autorun(function () {console.log(weatherResults.find({}, {sort: {_id: 1}}).fetch());});
  */
}
